package net.mtgsaber.smm.client.models

import java.nio.file.Path

/**
 * The metadata about a user's Minecraft installation, such as installation root.
 */
case class MCInstallationSpec(root: Path) {
  
}
